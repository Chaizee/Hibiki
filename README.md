# Hibiki

Voice wellness Flutter app (Resonant Sanctuary).

## Foundation

The `master` branch contains only the Flutter project scaffold (platform folders + minimal `main.dart`).

Feature work lands through stacked pull requests — see [docs/MERGE_ORDER.md](docs/MERGE_ORDER.md).

## Run

```bash
flutter pub get
flutter run
```
